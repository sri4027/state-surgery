# Source, licence and AI-assistance record

## Code
| Component | Origin | Licence |
|---|---|---|
| `arcstate/*`, `tools/*`, `web/engine.js`, `web/index_template.html` | written for this submission | MIT (this repo) |
| PyTorch | third-party dependency | BSD-3-Clause |
| NumPy | third-party dependency | BSD-3-Clause |
| ReportLab | used only to render the summary PDF | BSD |

**Nothing was forked or copied.** `pathwaycom/bdh` (MIT), `pathwaycom/arc-task-gen` (MIT) and
`lucidrains/bdh-cq` (MIT) were **read as references and cited**; no lines were taken from any of them.
The audit of `lucidrains/bdh-cq` is in `docs/BDH_DIFF.md`.

## Data and weights
| Asset | Origin |
|---|---|
| All tasks, grids, permutations | **synthetic**, generated by `arcstate/tasks.py` |
| `runs/main.pt`, `web/model.json` | trained by us from random init, seed 1234 |
| `web/results.json` | produced by `arcstate/experiments.py`, seed 20260908 |

No ARC-AGI, RE-ARC, ConceptARC, ARC-Heavy or ARC-GEN100K data was downloaded or used. No pretrained
weights of any kind were used. No BDH or BDH-CQ checkpoint was used — none is public.

## Graphics, fonts, assets
No external fonts, images, icons, CSS frameworks or JS libraries. System font stacks and inline SVG
only. The 10-colour grid palette follows the ARC convention. The artifact makes **no network requests**.

## AI assistance disclosure
AI assistance (Claude) was used throughout for research, code, debugging and prose: the model
implementation, task generator, training loop, experiment harness, browser forward pass, artifact and
all documentation. Every component is documented and defensible by the team. The debugging findings in
README §5 were produced by iterative diagnosis during this build, not taken from any source.

## Mentorship
None.
